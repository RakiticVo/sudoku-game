name: sudoku_game
version: 0.1.0
description: Pure Dart Sudoku engine and app state primitives.
environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  collection: ^1.18.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
